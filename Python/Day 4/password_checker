password = " "
attempts = 0
while password != "QuEeN" and attempts < 3:
    password = input("Enter the password: ")
    attempts += 1
    if password != "QuEeN" and attempts < 3:
        print(f"Wrong password! Attempts left: {3 - attempts}")
if password == "QuEeN":
    print("Access granted!")
else:
    print("You're blocked!")
    